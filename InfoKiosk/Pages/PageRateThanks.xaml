<Page x:Class="InfoKiosk.PageRateThanks"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:InfoKiosk"
      mc:Ignorable="d" 
      d:DesignHeight="911" 
      d:DesignWidth="1920"
      Title="PageRateThanks">
    <Grid x:Name="GridMain">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Image Grid.Row="1" 
               Source="/InfoKiosk;component/Media/ImageRateThanks.png"
               RenderOptions.BitmapScalingMode="HighQuality"
               HorizontalAlignment="Stretch"
               VerticalAlignment="Stretch" Stretch="UniformToFill"/>

        <TextBlock Grid.Row="2"
                   Text="Для продолжения коснитесь экрана" 
                   TextAlignment="Center" 
                   Margin="4" 
                   Foreground="DarkGray" 
                   FontSize="40"/>
    </Grid>
</Page>
